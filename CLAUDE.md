# Кто есть кто

Роли фиксированы ниже, в разделе «Кто за что отвечает», и одинаковы на
обеих машинах — своё имя в этом файле указывать не нужно, чтобы Даня и
Тимофей не перезаписывали друг друга при пуше.

---

# Что мы делаем

Нас двое, мы вместе делаем этот сайт. Каждый работает в своём Claude Code
на своей машине. Два Claude Code напрямую друг с другом не общаются и не
видят чужих сессий. Общаемся мы только через файлы в этом git-репозитории.

Git здесь - единственный источник правды. У каждого своя локальная копия
проекта, синхронизируемся мы через общий репозиторий на GitHub. То есть
функционально это одна общая папка, просто изменения переносятся через git.

Ветки: работаем в **dev**, `main` - стабильная ветка. В dev коммитим и
пушим напрямую (это наша общая рабочая ветка, тут можно и нужно часто
пушить). Из dev в main изменения переносятся только через Pull Request на
GitHub - не пушь и не мерджи в main напрямую.

Стек: **[ЗАПОЛНИТЬ - фронт и бэк, например React + Vite на фронте, Node
+ Express на бэке]**

---

# Кто за что отвечает

- **frontend/** - зона Тимофея
- **backend/** - зона Дани

Работай в своей зоне. Файлы в чужой зоне без явной причины не меняй. Если
кажется, что нужно тронуть чужую зону, сначала опиши это в docs/HANDOFF.md
и скажи об этом своему разработчику.

---

# Документы проекта

## Что мы строим (меняется редко)
- **docs/PROJECT.md** - цель, пользователь, стек, границы проекта. Если
  ты открыл репозиторий впервые - читай отсюда.
- **docs/MVP.md** - что входит в первую версию, а что осознанно не входит.
  Критерий готовности.
- **docs/STRUCTURE.md** - карта репозитория и правила раскладки файлов.

## Рабочие файлы (меняются постоянно)
- **TODO.md** - задачи обоих. Закрыл - отметь и перенеси в "Сделано".
- **docs/API.md** - контракт между фронтом и бэком: список эндпоинтов,
  форматы запросов и ответов. Это шов, где две части встречаются. Пока
  контракт не меняется, каждый работает независимо.
- **docs/PROGRESS.md** - лог изменений. Кто, когда, что сделал. Только
  дописываем, ничего не стираем. Новые записи добавляй СВЕРХУ, чтобы мы
  сами не конфликтовали в этом файле.
- **docs/HANDOFF.md** - прямые сообщения второму разработчику. Например:
  "поменял формат ответа /login, обнови обработку на фронте". Прочитал
  сообщение и выполнил - удали его.
- **docs/RECOMMENDATIONS.md** - советы и запасные планы. Упёрлись в
  проблему - сначала загляни сюда, план Б может быть уже написан.

---

# Навыки (.claude/skills/)

Общие для обеих машин, подхватываются автоматически:

- **project-docs** - держать документацию в соответствии с кодом
- **project-structure** - чистая структура, безопасный перенос файлов
- **test-project** - прогон работоспособности и безопасности
- **advisor** - рекомендации и запасные планы

---

# Правила для тебя, Claude Code

## В начале каждой сессии
1. Убедись, что ты на ветке `dev` (`git checkout dev`).
2. Сделай `git pull`, чтобы подтянуть изменения второго разработчика.
3. Прочитай docs/HANDOFF.md - есть ли сообщения лично для нас.
4. Прочитай docs/PROGRESS.md - что напарник сделал с прошлого раза.
5. Прочитай docs/API.md - актуальный контракт фронт/бэк.
6. Прочитай TODO.md - что в работе.

## После каждого заметного изменения
1. Допиши запись в НАЧАЛО docs/PROGRESS.md в формате:
   `ГГГГ-ММ-ДД | [имя] | что изменил | что напарнику важно знать`
2. Обнови TODO.md: закрытые задачи отметь, новые запиши.
3. Если появилась/переехала папка или значимый файл - обнови
   docs/STRUCTURE.md.
4. Если ты поменял контракт (эндпоинт, формат данных) или что-то, что
   затрагивает зону напарника - обнови docs/API.md и опиши изменение в
   docs/HANDOFF.md.

Документацию правь в том же коммите, что и код. Отдельной задачей это не
делается никогда.

## Перед завершением работы
1. Сначала `git pull` (вдруг напарник что-то залил, пока мы работали) - в
   ветке dev.
2. Потом `git commit` с понятным сообщением.
3. Потом `git push` (в dev).
4. Если git ругается на конфликт - не решай молча. Покажи мне конфликт и
   предложи вариант решения.

## Перенос dev → main
Когда набралась стабильная порция изменений (по моей команде, не сам по
себе):
1. Прогони навык `test-project` - красный прогон в main не заливаем.
2. Открой Pull Request dev → main (`gh pr create --base main --head dev`),
   опиши, что вошло.
3. Решение о слиянии - за человеком. Сам не мерджи.

## Чего делать нельзя
- Пушить и мерджить в `main` напрямую, минуя PR.
- Класть секреты (токены, ключи, пароли) в отслеживаемые файлы. Только
  `.env`, а в репозиторий - `.env.example` с пустыми значениями.
- Молча менять docs/API.md - это ломает работу напарника.
- Молча чинить чужую зону.
- Отмечать в отчёте о тестах непроверенное как пройденное.

---

# Коротко

Мы двое делаем лендинг через два отдельных Claude Code. Синхронизация -
через git и файлы в docs/. Читай почту (HANDOFF.md, PROGRESS.md, API.md) в
начале, пиши в неё в конце, держи свою зону, работай в ветке dev. Так мы
всегда в курсе того, что делает другой, и не мешаем друг другу.

Документация врёт - значит, её никто не обновил в том же коммите. Не
доводи до этого.
