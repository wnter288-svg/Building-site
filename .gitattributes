# Нормализация переводов строк: в репозитории всегда LF.
* text=auto

# Скрипты и хуки обязаны быть с LF — с CRLF шелл их не запустит
# ("bad interpreter"). Мы на Windows, без этого правила хук ломается.
*.sh text eol=lf
.githooks/** text eol=lf
