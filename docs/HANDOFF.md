# Handoff

Прямые сообщения второму разработчику. Прочитал — выполнил — удали
запись. Новые сообщения дописывай в конец.

---

## Тимофею от Дани — крупные изменения, прочитай до начала работы

**1. Лендинг DIMORA свёрстан.** По макету из Claude Design, вместе с
дизайн-системой. Лежит в `frontend/`, запускается `npm run dev`.

**2. Стек уточнён: Astro остался, Express — нет.**

Ты выбрал «Astro + Express», Astro мы оставили. А вот отдельного Express
не будет: обработчик заявки живёт внутри Astro
(`src/pages/api/lead.ts`) и выполняется на сервере Vercel.

Причина: форма шлёт заявки в Telegram, а токен бота нельзя отдавать в
браузер — любой посетитель заберёт бота. Значит нужен сервер. Поднимать
ради одного эндпоинта отдельный сервис и второй хостинг — лишняя работа
и второе место отказа.

**3. GitHub Pages выключен, публикуемся на Vercel.** Pages отдаёт только
статику, серверного обработчика там нет. Workflow `deploy-pages.yml`
удалён, инструкция по Vercel — в `docs/DEPLOY.md`.

**4. Это ломает деление зон, и решать нам вдвоём.** По `CLAUDE.md`
`frontend/` — твоя зона, `backend/` — моя. Сейчас `backend/` пустая, а
весь код в твоей. Варианты:
   - поделить внутри `frontend/`: на мне `lib/`, `pages/api/`, сборка и
     деплой, на тебе — секции и стили;
   - вернуть отдельный Express, если хочется тренировать именно две
     части (но тогда второй хостинг и CORS);
   - переписать раздел «Кто за что отвечает» под реальность.

Мой голос за первый. Скажи, что думаешь, — без твоего согласия
`CLAUDE.md` не трогаю.

**5. Что посмотреть в коде:**
   - `src/content/site.ts` — весь текст сайта, там же заглушки `todo`
     (телефон, почта, сроки, имена основателей). В компонентах строк нет.
   - `src/styles/tokens/` — токены из дизайн-системы, перенесены без
     изменений. Цвета и кегли только отсюда.
   - Секция «Проєкти» показывается, карточки пока с заглушками вместо
     фотографий. Выключается одной строкой: `showProjects` в `site.ts`.
   - Логотип в кривых, поэтому Cinzel не подключается вовсе — минус один
     шрифт из трёх.

---

## Тимофею от Дани — я сделал страницу 404, это твоя зона

Задача числилась за мной (SEO в `TODO.md`), но файл лёг во `frontend/`.
Не спросил заранее — говорю сейчас. Не нравится решение — переделывай
свободно, спорить не буду.

**Что появилось:** `frontend/src/pages/404.astro`. Фотография во весь
экран (`public/404.jpg`, 1829×860, 96 КБ), текст лежит поверх неё слева.

**Три места, которые тебя касаются:**

1. **У 404 своя шапка, а не `Header.astro`.** Твоя шапка красит логотип
   и ссылки Льном — она рассчитана на тёмное видео первого экрана. На
   светлой странице ошибки она была бы невидимой. В `404.astro` лежит
   упрощённая: логотип и две ссылки, без телефона, CTA и бургера.
   Если будешь менять шапку — эта не подхватит изменения, поправь обе.

2. **В `Layout.astro` добавился проп `noindex`.** Он выключает
   `canonical` и ставит `<meta name="robots" content="noindex, follow">`.
   На обычных страницах ничего не меняется — по умолчанию `false`.

3. **Свой JS на страницу не подключён.** Ошибочный адрес — плохой момент
   зависеть от бандла. Появление строк заголовка делает CSS-анимация
   `.hero-lines + .lm` из `global.css` — та же, что в hero. Если будешь
   трогать эту анимацию, помни, что на неё завязана ещё и 404.

4. **Контраст текста на снимке посчитан, а не прикинут на глаз.** Самая
   тёмная точка в зоне текста — 0.583 яркости, это Хвоя 9.6:1 и Мох
   4.57:1. Мох проходит норму 4.5 с запасом в сотые, поэтому мелкий
   текст на странице набран `--text-body` (7.07:1), а настоящий Мох
   оставлен только крупному «404», где норма 3.0. Кадр держится
   привязкой слева: пустое небо остаётся под текстом. Поменяешь снимок
   или привязку — пересчитай, цифры перестанут действовать.

**Текст** — в `site.ts`, блок `notFound`, снимок там же в `notFound.photo`.
