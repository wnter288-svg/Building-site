# Progress

Лог изменений. Новые записи добавляем сверху, ничего не стираем.

Формат: `ГГГГ-ММ-ДД | [имя] | что изменил | что напарнику важно знать`

2026-09-09 | Тимофей | Переделал «Послуги»: ступенчатая раскладка с непрерывной линией-маршрутом вместо трёх равных колонок, подсветка при наведении. Фон замощён компактным знаком по диагонали. Поднял яркость текста в тёмных секциях | ВАЖНО: в знаке был перекос — буква D стояла левее центра дома на 13.94, поправил и в `mark.svg`, и в `favicon.svg`. Перекос пришёл из дизайн-системы, при следующем импорте макета вылезет снова. Ещё: тронул `tokens/colors.css` — владелец согласовал, подробности в HANDOFF. Закрыт твой пункт QA про `--text-faint` (3.47 → 4.97)

2026-09-09 | Тимофей | Убрал видео с первого экрана — вместо него одна фотография `hero.jpg` (265 КБ вместо 4.65 МБ). Первый экран переведён в светлую тему: тёмный текст вместо Льна, затемнение заменено засветкой. Шапка стала двухрежимной | ВАЖНО про шапку: цвет теперь берётся из `--head-fg`, а не из `--c-len` напрямую — тёмный, пока она прозрачная над светлым фото, прежний Лён, когда после прокрутки становится тёмной плашкой. Мобильное меню не трогал. Закрыл четыре твоих пункта из QA: вес hero.mp4, подпись под роликом 2.62, «Скрол» 4.06, навигация шапки. Весь контраст первого экрана пересчитан по фотографии — минимум 4.44 при норме 3.0 для крупного текста и 5.39 при норме 4.5 для мелкого

2026-09-09 | Даня | Подключил снимки в «Здані обʼєкти»: три сгенерированных кадра 800×1200 (`project-1..3.jpg`, 95/85/141 КБ). Карточки теперь рисуют <img> вместо пустой заглушки | ТВОЯ ЗОНА, задача была за тобой в TODO — переделывай свободно. Подпись поверх кадра (`pc__caption`) больше не выводится: это было задание на съёмку, а не текст сайта, и на живом снимке она провалилась бы по контрасту — набрана --text-faint без подложки. Названия и площади объектов всё ещё заглушки. Кадры готовил под слой параллакса: он выше рамки на 20%, поэтому в покое видно только середину по высоте

2026-09-09 | Даня | Переделал 404 по присланному кадру: фотография (`public/404.jpg`, 1829×860, 96 КБ) во весь экран, текст поверх неё, сдвинут левее контейнера 1280. Залитая кнопка заменена ссылкой со стрелкой | Контраст на снимке посчитан: худшая точка в зоне текста 0.583 яркости, Хвоя 9.6:1, Мох 4.57:1. Мелкий текст поэтому набран --text-body, а не Мхом — у Мха запас в сотые. Кадр привязан слева, там пустое небо. Поменяешь снимок — пересчитай. Навигация стоит рядом с логотипом, а не у правого края: правый верхний угол снимка занят кроной, и при пропорциях окна около 1900x1000 она встаёт ровно под «Проєкти»

2026-09-09 | Даня | Сделал страницу 404 (`frontend/src/pages/404.astro`) по присланному макету: слева текст, справа кадр в край экрана. Текст в `site.ts`, блок `notFound`. В `Layout.astro` добавился проп `noindex` | ЗАШЁЛ В ТВОЮ ЗОНУ — задача числилась за мной (SEO в TODO), но легла во `frontend/`, подробности в HANDOFF.md. У 404 своя облегчённая шапка: `Header.astro` светит Льном по тёмному видео и на светлой странице был бы невидим. Своего JS на странице нет — анимация заголовка чисто на CSS

2026-09-08 | Даня | Полный QA-прогон: работоспособность, безопасность, доступность, SEO, производительность, гигиена репозитория. Все находки занесены в TODO.md, раздел «По итогам QA-прогона» | Ничего не чинил и не удалял — только зафиксировал. Главное: заявка не доходит до адресата (нет токена Telegram), 6 провалов контраста, нет своей 404 и заголовков безопасности. Что уже проверено и работает — отдельным списком в конце раздела, перепроверять не нужно

2026-09-08 | Даня | Переделал секцию «Про компанію»: фото в край экрана слева, заголовок поверх него на затемняющей плашке, текст справа. Появление блоков теперь повторяется при каждом заходе в кадр, а не один раз за визит. Выровнял логотип и текст на фото по контейнеру страницы | Отступ шапки переехал с .bar на .dm-head — если правишь шапку, учитывай. Появилась переменная --sbw (ширина полосы прокрутки), её ставит trackScrollbar() в behaviors.ts

2026-09-08 | Тимофей | Пересобрал ролик первого экрана — убрал дрожание картинки. Камера теперь неподвижна всю стройку (кадры статичные, зума нет), перетекания удлинены до 1.4 с, подъезд камеры остался только в финале и считается в разрешении 4800 px, чтобы не было ступенчатого зума | hero.mp4: 22.1 с, 1600×900, 4.8 МБ. Левая половина кадра во всех сценах намеренно пустая — там лежит заголовок, учитывай при правках hero

2026-09-07 | Тимофей | Заменил ролик в первом экране: hero.mp4 и poster.jpg. Новый таймлапс — 12 кадров реальной технологии стройки (лента с каркасом Ø8, полы по грунту, газоблок на клей, армопояс газоблок+OSB, монолитное перекрытие, стропила, металлочерепица), дом двухэтажный барнхаус, финал — готовый дом в снегу | Старый ролик был технически неверным (бетон, потом дерево, дом недостроен). Ролик 24.6 с, 1600×900, 4.3 МБ вместо 2.4 МБ. Композиция кадров держит левую половину пустой под заголовок — при правках вёрстки hero это учитывай

2026-09-07 | Даня | Свёрстан лендинг DIMORA по макету из Claude Design: Astro + TypeScript, 10 секций, форма заявки с отправкой в Telegram через /api/lead. Деплой переведён с GitHub Pages на Vercel, deploy-pages.yml удалён | Express не понадобился — обработчик внутри Astro. Это ломает деление зон frontend/backend, вариант решения в HANDOFF.md, жду твоего ответа

2026-09-02 | Тимофей | Выбрал стек: Astro + Express (принял вариант Дани), зафиксировал в PROJECT.md, закрыл блокер в TODO.md, ответил в HANDOFF | Блокер по стеку снят — можешь начинать бэк; дальше инициализирую Astro в frontend/

2026-09-02 | Даня | Проверил проект целиком, убрал хвосты: переписал HANDOFF.md (было 6 сообщений вперемешку, одно с устаревшей неправдой про защиту веток), убрал дубли и выполненные задачи из TODO.md, добавил DEPLOY.md в CLAUDE.md, поправил README | В HANDOFF.md теперь два блока: что сделать тебе и открытый вопрос про стек

2026-09-02 | Даня | Проект переведён на учебный режим: репозиторий публичный, сайт на GitHub Pages, branch protection включена, деплой на VPS отменён | Сайт: https://wnter288-svg.github.io/Building-site/ — ВАЖНО: в сборщике нужен `base: '/Building-site/'`, иначе стили дадут 404, см. docs/DEPLOY.md

2026-09-02 | Даня | Написал автодеплой: frontend → Cloudflare Pages, backend → VPS по SSH. Инструкция по подключению в docs/DEPLOY.md | Деплой идёт ТОЛЬКО из main после мерджа PR; пуш в dev на сайт ничего не выкатывает. Сборка фронта должна лежать в frontend/dist

2026-09-02 | Даня | Уточнил рынок: сайт на Украину. Поправил документы — GA4 вместо Яндекс.Метрики, домен .ua, телефоны +380 | ВАЖНО для вёрстки: украинская версия должна грузиться по умолчанию (требование закона), см. docs/MVP.md

2026-09-02 | Даня | Добавил хук .githooks/pre-push (блокирует push в main) и .gitattributes; branch protection на GitHub недоступна на бесплатном плане для приватных реп | ОБЯЗАТЕЛЬНО выполни у себя: `git config core.hooksPath .githooks`

2026-09-02 | Даня | Завёл документацию (PROJECT, MVP, STRUCTURE, TODO, RECOMMENDATIONS, README), 4 навыка Claude Code в .claude/skills/, .gitignore, CI на GitHub Actions, папки frontend/ и backend/ | Прочитай docs/PROJECT.md и TODO.md; твоя зона — frontend/, задачи там же

2026-09-02 | Даня | Создал ветку dev, обновил CLAUDE.md: работаем в dev, в main переносим только через PR | Переключись на dev (`git checkout dev`) и работай там, main трогать не нужно

2026-08-29 | Даня | Убрал из CLAUDE.md строку "Разработчик на этой машине" (мы её оба перезаписывали друг у друга при пуше) — теперь роли берём только из секции «Кто за что отвечает» | Строку своё имя больше вписывать не нужно
2026-08-29 | Тимофей | Вписал себя в CLAUDE.md (frontend), предложил 4 варианта стека в HANDOFF.md | Стек ещё НЕ выбран, жду твой ответ по бэку; заглушку в CLAUDE.md не трогал
2026-08-29 | Даня | Заполнил CLAUDE.md (имя, зона), создал docs/API.md, docs/PROGRESS.md, docs/HANDOFF.md | Стек ещё не выбран, см. CLAUDE.md
