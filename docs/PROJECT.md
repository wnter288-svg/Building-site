# Проект

Главный документ о том, что мы делаем и зачем. Если ты (человек или ИИ)
открыл репозиторий впервые — начни отсюда, потом читай docs/STRUCTURE.md.

---

## Что это

Лендинг для строительной компании. Одностраничный (или несколько страниц)
сайт-презентация: рассказать об услугах, показать выполненные объекты,
получить заявку от клиента.

**Название компании:** [ЗАПОЛНИТЬ]
**Страна:** Украина
**Регион/город работы:** [ЗАПОЛНИТЬ]
**Сайт/домен:** [ЗАПОЛНИТЬ — ещё не куплен, ориентир `.ua` / `.com.ua`]
**Язык сайта:** украинский по умолчанию (требование закона, см. docs/MVP.md)

## Зачем (цель)

Главная бизнес-цель — **получать заявки**. Всё остальное на сайте работает
на неё: тексты, фото объектов, отзывы, кнопки. Метрика успеха — количество
оставленных заявок, а не красота или количество страниц.

Вторая цель — **выглядеть надёжно**. Строительство — покупка на большие
деньги и надолго, клиент выбирает того, кому доверяет. Отсюда: реальные
фото объектов, конкретика вместо общих слов, контакты на виду.

## Кто пользователь

Человек, который планирует стройку или ремонт и ищет подрядчика. Скорее
всего:
- пришёл из поиска («строительство домов [регион]») или из рекламы;
- смотрит с телефона;
- сравнивает 3-5 компаний одновременно;
- хочет быстро понять: что вы строите, сколько это стоит, кому звонить.

Отсюда требования: быстрая загрузка, хорошее отображение на мобильном,
цены/вилки цен, контакт в один клик.

## Стек

**Предварительно:** Astro (фронт) + Node/Express (бэк).
**Статус:** не финализирован — ждём подтверждения Тимофея, см.
docs/HANDOFF.md.

Обоснование выбора Astro: лендингу критичны SEO и скорость загрузки, а
интерактива мало. Astro отдаёт статический HTML и не грузит лишний JS.
Интерактивные блоки (галерея, калькулятор) при необходимости добавляются
точечно как «острова».

Обоснование Express: бэк здесь маленький — приём заявки из формы и
уведомление. Тяжёлый фреймворк не нужен.

## Команда

- **Тимофей** — frontend (`frontend/`)
- **Даня** — backend (`backend/`) + инфраструктура (хостинг, домен, деплой,
  аналитика, интеграции)

Работаем в двух отдельных Claude Code, синхронизируемся через git.
Подробности процесса — в CLAUDE.md.

## Границы проекта

**Это НЕ:**
- интернет-магазин;
- личный кабинет клиента;
- CRM (заявки уходят во внешний канал — почта/Telegram/CRM);
- блог с большой редакцией (пара статей для SEO — максимум).

Если появляется задача из этого списка — это отдельный проект или
следующий этап, не MVP. Сначала обсуждаем, потом делаем.

---

Что входит в первую версию — см. docs/MVP.md.
Текущие задачи — см. TODO.md.
